<h2 id="Bosses">Bosses</h2>

<p>Due to a probable bug in the game War and Satan bosses always appear as not
being beaten.</p>

<table class="table table-hover table-condensed">
    <col width="10%" />
    <col width="70%" />
    <col width="20%" />
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th class="text-center">Defeated</th>
    </tr>
    {% for bossID, bool in save.bosses %}
        <tr>
            <td>{{ bossID }}</td>
            <td>{{ catalogue.bosses[bossID] }}</td>
            <td class="text-center">
                {% if save.bosses[bossID] %}
                    <span class="glyphicon glyphicon-ok text-success"></span>
                {% endif %}
            </td>
        </tr>
    {% endfor %}
</table>
